'use strict';

/**
 * @module ui/status.view
 * Рендер нижней карточки таймера: подпись «кто думает» + таймер + кнопка паузы.
 * Верхней текстовой «плашки» фазы больше нет: чей ход показывает неоновая
 * подсветка карточки команды (task 4), победителя — заливка цветом
 * (ui/modals.renderWin), а кнопки лобби/итога — нижняя карточка #game-controls.
 */

import { $ } from '../util/dom.js';
import { getState } from '../state/store.js';

/**
 * Перерисовывает нижнюю карточку таймера. Она видна только в активной игре:
 * подпись «Лидер думает:» (фаза clue — ход капитана) либо «Команда думает:»
 * (фаза guess — команда угадывает), таймер и кнопка паузы. На паузе подпись и
 * значение таймера не меняются (показывают обычный текст и оставшееся время),
 * переключается только подпись кнопки «⏸ Пауза» ↔ «▶️ Возобновить» — стиль кнопки
 * (btn-ghost) единый. Вызывается на каждый тик — таймер обновляется ежесекундно.
 * @returns {void}
 */
export function renderStatus() {
  const state = getState();
  const inGame = state.phase === 'clue' || state.phase === 'guess';

  // Нижняя карточка таймера — только в активной игре.
  $('#timer-bar').classList.toggle('hidden', !inGame);
  if (inGame) {
    // clue — думает капитан (даёт подсказку), guess — угадывает команда.
    // На паузе подпись и таймер остаются обычными (без «⏸ Пауза» слева и иконки
    // паузы справа) — текущее оставшееся время видно и во время паузы.
    $('#turn-label').textContent = state.phase === 'clue' ? 'Лидер думает:' : 'Команда думает:';
    $('#timer-val').textContent = state.timer;
    $('#timer').classList.toggle('low', state.timer <= 10);
    // Одна кнопка-переключатель: меняется только подпись, стиль (btn-ghost)
    // одинаков для «Пауза» и «Возобновить».
    $('#pause-btn').textContent = state.paused ? '▶️ Возобновить' : '⏸ Пауза';
  }
}
